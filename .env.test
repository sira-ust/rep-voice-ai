# Only what differs when working against a copy of the agent.
# Read only when you ask for it:
#     APP_ENV=test python databricks_tool.py --sync
#
# Everything else -- keys, tokens, auth -- comes from .env, so there is
# one copy of each secret rather than two that can disagree.
ELEVENLABS_AGENT_ID=agent_4101m3bc7hf3fmpaz3yncz2w6g2v
